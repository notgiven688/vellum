#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")"

PORT="${PORT:-8080}"
HOST="${HOST:-localhost}"

rm -rf _site
rm -rf api

docfx metadata docfx.json
docfx build docfx.json

echo "Serving Vellum docs at http://${HOST}:${PORT}"
docfx serve _site --hostname "$HOST" --port "$PORT"
